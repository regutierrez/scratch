Report the deployment status of the `checkout-api` service as a JSON object with exactly these keys:

- `service` (string)
- `healthy` (boolean)
- `replicas` (integer)
- `checkedAt` (ISO-8601 timestamp)

Output JSON only. Do not add prose before or after the object.
